apiVersion: v1
kind: Service
metadata:
  name: {{ include "mock-idp.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "mock-idp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "mock-idp.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
